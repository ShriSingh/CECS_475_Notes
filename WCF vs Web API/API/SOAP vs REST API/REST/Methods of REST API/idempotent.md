#CECS475 
#Week7 
An HTTP method that can be called many times without different outcomes
- Wouldn't matter if the method is called only once, or ten times over, the **result should be the same**
	- Only applies to the results, *not the resource itself*