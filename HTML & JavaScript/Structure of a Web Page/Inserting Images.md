#CECS475 #Week8 
- Type `<IMG SRC="image.ext">`
	- Where `image.ext` indicates the location of the image file
- `WIDTH=n` and `HEIGHT=n` attributes can be used to adjust the size of an image
- Attribute `BORDER=n` can be used to add a border `n` pixels thick around the image