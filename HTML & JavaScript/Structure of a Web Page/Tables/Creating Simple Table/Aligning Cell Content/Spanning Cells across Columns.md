#CECS475 #Week8 
It is often necessary to span one cell across many columns
- For example:
	- You would use this technique to span a headline across the columns of a newspaper article
- To span a cell across many columns, type `<TD COLSPAN=n>`, where `n` is the number of columns to be spanned