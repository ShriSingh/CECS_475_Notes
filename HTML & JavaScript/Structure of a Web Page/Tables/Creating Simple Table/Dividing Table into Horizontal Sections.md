#CECS475 #Week8 
You can also create a horizontal section consisting of one or more rows
- This allows you to format the rows all at once

To create a horizontal section, type `<THEAD>`, `<TBODY>`, `<TFOOT>` before the first `<TR>` tag of the section

**NOTE:** Netscape does not support these tags