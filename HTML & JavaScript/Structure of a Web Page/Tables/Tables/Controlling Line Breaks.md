#CECS475 #Week8 
Unless you specify otherwise a browser will divide the liens in a cell as it sees fit
- The `NOWRAP` attribute places within the `<TD>` tag forces the browser to keep all the text in a cell on one line
Example:
```HTML
<TD NOWRAP>Washington, D.C.
```