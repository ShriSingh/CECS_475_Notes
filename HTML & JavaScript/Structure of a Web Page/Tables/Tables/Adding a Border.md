#CECS475 #Week8 
The `BORDER=n` attribute allows you to add a border `n` pixels thick around the table
- To make a solid boarder color, use the `BORDERCOLOR="color"` attribute
- To make a shaded colored border, use the `BORDERCOLORDARK="color"` and `BORDERCOLORLIGHT="color"`