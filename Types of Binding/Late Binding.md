#CECS475 
#Week6 
Associated with the formation of code at run time
- If a chain of virtual methods is found in the class hierarchy, the compiler builds the so-called late binding
	- Using the word `virtual`, `override`
- Method is called based on the type of the object, **not the type of reference to the base class**
- Used when **polymorphism** needs to be implemented
